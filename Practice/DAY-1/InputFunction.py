# Print the length of the name entered
# input is a function which takes the input from the user and returns the value
name = input("What is your name?\n")
# printing the length of the name using len() function
print(len(name))